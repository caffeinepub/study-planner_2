import * as React from 'react';

// Minimal Toaster component that doesn't break the build
// This is a placeholder that can be replaced with a full implementation later
export function Toaster() {
  return null;
}
